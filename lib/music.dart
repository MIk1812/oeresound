import 'package:flutter/material.dart';
import 'package:oeresound/theme.dart';

class Music extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Music", style: title),
    );
  }
}
