import 'package:flutter/material.dart';
import 'package:oeresound/theme.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Home", style: title),
    );
  }
}
